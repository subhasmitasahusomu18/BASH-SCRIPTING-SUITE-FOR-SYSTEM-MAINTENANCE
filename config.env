# === Suite Configuration (personalized defaults) ===
# Adjust as needed before running on your machine.

# Path to suite log
SUITE_LOG="${SUITE_LOG:-$(pwd)/suite.log}"

# === Backup settings ===
# Back up Documents and Pictures by default (add/remove paths as needed)
BACKUP_SRC="${BACKUP_SRC:-$HOME/Documents $HOME/Pictures}"
# Destination directory (ensure it exists and is writable; change for your system)
BACKUP_DEST="${BACKUP_DEST:-/tmp/backups}"
# Optional exclude patterns file
BACKUP_EXCLUDES_FILE="${BACKUP_EXCLUDES_FILE:-$(pwd)/exclude.example.txt}"
# Retention in days
BACKUP_RETAIN_DAYS="${BACKUP_RETAIN_DAYS:-14}"
# Default to dry-run to avoid accidental large copies; pass --no-dry-run to actually copy
BACKUP_DRYRUN_DEFAULT="${BACKUP_DRYRUN_DEFAULT:-true}"

# === Update settings ===
UPDATES_YES="${UPDATES_YES:-true}"

# === Log monitor thresholds ===
DISK_USAGE_WARN="${DISK_USAGE_WARN:-85}"
TOP_CPU_N="${TOP_CPU_N:-5}"
WATCH_INTERVAL="${WATCH_INTERVAL:-10}"
